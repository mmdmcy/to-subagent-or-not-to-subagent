# Harness-Weight Light Fixture

This public fixture is the small maintenance task used by the harness-weight
follow-up. Implement `retryWithBackoff` in `src/retry-with-backoff.js` and run
the public tests. Hidden checks are intentionally not included.
