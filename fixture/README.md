# Independent Package Workload

Complete all three independent tasks:

1. `implementation/README.md`
2. `security/README.md`
3. `comprehension/README.md`

The packages do not share files or dependencies. Run each package's available checks. Finish only after all three package tasks are complete.

# Benchmark Fixture

This is an intentionally incomplete benchmark input. The security package is
deliberately vulnerable and its untouched public tests are expected to fail.
Do not deploy any code in this directory.
